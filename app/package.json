{
  "name": "app",
  "version": "1.0.0",
  "description": "Gilded Rose kata in TypeScript",
  "main": "dist/src/index.js",
  "module": "dist/src/index.es.js",
  "types": "dist/src/index.d.ts",
  "scripts": {
    "precompile": "rimraf src/**/*.js",
    "compile": "tsc",
    "build": "tsc --build",
    "pretest": "rimraf src/**/*.js test/**/*.js",
    "test": "jest",
    "test:watch": "jest --watchAll"
  },
  "license": "MIT",
  "private": true,
  "devDependencies": {
    "@types/jest": "^26.0.15",
    "@types/node": "^16.11.7",
    "jest": "^27.3.1",
    "rimraf": "~3.0.2",
    "source-map-support": "^0.5.20",
    "ts-jest": "^27.0.7",
    "ts-node": "^10.4.0",
    "tsconfig-paths": "^3.11.0",
    "typescript": "^4.4.4"
  }
}
